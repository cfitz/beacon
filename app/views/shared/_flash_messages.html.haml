- flash.each do |type, message|
  %div.alert.alert-message.fade.in{ "data-alert" => "alert", :class => twitterized_type(type) }
    %button{ :type => "button", :class => "close", :data => { :dismiss => "alert"}} x
    %p
      = message