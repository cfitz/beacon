= form_for @annotation do |f|
  -if @annotation.errors.any?
    #error_explanation
      %h2= "#{pluralize(@annotation.errors.count, "error")} prohibited this annotation from being saved:"
      %ul
        - @annotation.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_field :content
  .actions
    = f.submit 'Save'
