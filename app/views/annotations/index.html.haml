%h1 Listing annotations

%table
  %tr
    %th Title
    %th Content
    %th
    %th
    %th

  - @annotations.each do |annotation|
    %tr
      %td= annotation.title
      %td= annotation.content
      %td= link_to 'Show', annotation
      %td= link_to 'Edit', edit_annotation_path(annotation)
      %td= link_to 'Destroy', annotation, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Annotation', new_annotation_path
