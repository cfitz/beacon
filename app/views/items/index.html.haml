- model_class = Item
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:uri)
      %th= model_class.human_attribute_name(:format)
      %th= model_class.human_attribute_name(:attachment_file_name)
      %th= model_class.human_attribute_name(:attachment_content_type)
      %th= model_class.human_attribute_name(:attachment_file_size)
      %th= model_class.human_attribute_name(:attachment_updated_at)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @items.each do |item|
      %tr
        %td= link_to item.id, item_path(item)
        %td= item.uri
        %td= item.format
        %td= item.attachment_file_name
        %td= item.attachment_content_type
        %td= item.attachment_file_size
        %td= item.attachment_updated_at
        %td=l item.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_item_path(item), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), item_path(item), :method => :delete, :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')), :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_item_path, :class => 'btn btn-primary'
