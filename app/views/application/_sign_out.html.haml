%li#signout{:class=> 'dropdown pull-right'}
  %a{ :href=> '#', :class=>'dropdown-toggle', :data => { :toggle => 'dropdown' } }
    Hello, #{current_user.email}
    %b{ :class=> 'caret'}
  %ul{ :class=>'dropdown-menu'}
    %li #{ link_to('Sign Out', destroy_user_session_path, :method => :delete) }